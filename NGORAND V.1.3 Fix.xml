<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE html>
<html b:css='false' b:defaultwidgetversion='2' b:layoutsVersion='3' b:responsive='true' b:templateUrl='indie.xml' b:templateVersion='1.0.0' expr:dir='data:blog.languageDirection' xmlns='http://www.w3.org/1999/xhtml' xmlns:b='http://www.google.com/2005/gml/b' xmlns:data='http://www.google.com/2005/gml/data' xmlns:expr='http://www.google.com/2005/gml/expr'>
<head>
  <meta content='width=device-width, initial-scale=1' name='viewport'/>
  <title><data:view.title.escaped/></title>
  <b:include data='blog' name='all-head-content'/>

  <!-- FONTS: Playfair Display (Judul Klasik) & Merriweather (Teks Novel) -->
  <link href='https://fonts.googleapis.com/css2?family=Merriweather:ital,wght@0,300;0,400;0,700;1,300&amp;family=Playfair+Display:wght@400;700&amp;display=swap' rel='stylesheet'/>

  <b:skin><![CDATA[
  /* -----------------------------------------------
  THEME: NGORAND
  STYLE: ELEGANT, VINTAGE, WITH 3D ANIMATIONS
  BY   : KURNIADITYA GUMELAR
  -----------------------------------------------
  */

  /* --- VARIABLES --- */
  :root {
      --bg-paper: #fcf8e3; /* Warna Cream / Kertas Tua */
      --ink-black: #2c2c2c;
      --retro-white: #ffffff;
      --highlight: #d4af37;
      --border-thick: 2px solid var(--ink-black);
      --font-head: 'Playfair Display', serif;
      --font-body: 'Merriweather', serif;
  }

  /* --- RESET & BASIC --- */
  body {
      background-color: var(--bg-paper);
      color: var(--ink-black);
      font-family: var(--font-body);
      margin: 0;
      padding: 0;
      font-size: 16px;
      line-height: 1.8;
      background-image: radial-gradient(#d4d0b8 1px, transparent 1px);
      background-size: 20px 20px;
      overflow-x: hidden;
      
      /* SECURITY: MENCEGAH SELEKSI TEKS */
      -webkit-user-select: none;
      -moz-user-select: none;
      -ms-user-select: none;
      user-select: none;
  }

  a { text-decoration: none; color: var(--ink-black); transition: all 0.3s cubic-bezier(0.175, 0.885, 0.32, 1.275); }
  
  /* PERBAIKAN IMAGE & ICON: HAPUS BORDER */
  img { 
      max-width: 100%; 
      height: auto; 
      display: block; 
      border: none; /* Hapus border kotak */
      border-radius: 0; /* Hapus radius default */
      background: transparent; /* Pastikan background transparan */
  }

  /* --- ANIMASI 3D KEYFRAMES --- */
  @keyframes float3D {
      0%, 100% { transform: translateY(0) rotateX(0) rotateY(0); }
      50% { transform: translateY(-10px) rotateX(5deg) rotateY(5deg); }
  }

  @keyframes pop3D {
      0% { opacity: 0; transform: scale(0.5) translateY(50px) rotateX(45deg); }
      70% { transform: scale(1.05) translateY(-10px) rotateX(-5deg); }
      100% { opacity: 1; transform: scale(1) translateY(0) rotateX(0); }
  }

  @keyframes slideIn3D {
      0% { opacity: 0; transform: translateY(50px) rotateX(10deg); }
      100% { opacity: 1; transform: translateY(0) rotateX(0); }
  }
  
  /* Animasi Halus untuk Popup */
  @keyframes smoothUp {
      0% { opacity: 0; transform: translateY(20px) scale(0.95); }
      100% { opacity: 1; transform: translateY(0) scale(1); }
  }

  /* --- LAYOUT CONTAINER --- */
  .main-wrapper {
      max-width: 800px;
      margin: 20px auto;
      padding: 0 20px;
      perspective: 1000px;
  }

  /* --- HEADER (Transparent Background) --- */
  .header-wrapper {
      text-align: center;
      margin-bottom: 20px;
      padding: 30px 0;
      background: transparent; /* KEMBALI TRANSPARAN */
      border-bottom: 3px double var(--ink-black);
      border-top: 1px solid rgba(0,0,0,0.05);
      animation: slideIn3D 1s ease-out;
      box-shadow: none; /* HAPUS SHADOW KOTAK */
      position: relative;
      z-index: 5;
  }

  .Header h1 {
      font-family: var(--font-head);
      font-size: 42px;
      margin: 0;
      letter-spacing: 1px;
      font-weight: 700;
      color: var(--ink-black);
      text-shadow: 2px 2px 0px rgba(0,0,0,0.05);
  }
  
  .Header p.description {
      font-family: var(--font-body);
      font-style: italic;
      margin-top: 5px;
      font-size: 16px;
      color: #555;
  }

  /* --- NAVIGATION (MODIFIED FOR DESKTOP & MOBILE) --- */
  
  /* 1. TAMPILAN DESKTOP (PC) - RAPI & SATU BARIS */
  @media screen and (min-width: 601px) {
      .nav-wrapper {
          position: relative;
          background: transparent;
          margin-bottom: 30px;
          text-align: center;
          border-bottom: none; /* HAPUS GARIS BAWAH */
          padding-bottom: 15px;
          perspective: 1000px;
      }

      .PageList ul {
          list-style: none;
          padding: 0;
          margin: 0;
          display: flex;
          flex-wrap: wrap;
          justify-content: center;
          align-items: center;
          gap: 8px; /* Jarak antar tombol diperkecil */
      }

      .PageList li a {
          display: inline-block;
          white-space: nowrap; /* Teks dilarang turun baris */
          /* UKURAN DIPERKECIL */
          padding: 8px 18px; 
          font-size: 11px;
          background: var(--retro-white);
          border: 1px solid var(--ink-black);
          border-radius: 50px;
          font-family: var(--font-body);
          font-weight: 700;
          text-transform: uppercase;
          letter-spacing: 1px;
          box-shadow: 0 3px 0 var(--ink-black); /* Shadow diperkecil */
          transform-style: preserve-3d;
          transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
          position: relative;
          top: 0;
          color: var(--ink-black);
      }

      .PageList li a:active { top: 4px; box-shadow: 0 0 0 var(--ink-black); }
      .PageList li a:hover {
          background: var(--ink-black);
          color: var(--retro-white);
          transform: translateY(-2px);
          box-shadow: 0 5px 0 rgba(0,0,0,0.2);
      }
      .PageList li.selected a {
          background: var(--ink-black);
          color: var(--retro-white);
          box-shadow: 0 2px 0 rgba(0,0,0,0.5);
          top: 2px;
      }
      
      /* Sembunyikan elemen mobile di PC */
      #mobile-floating-menu-btn, #mobile-menu-overlay { display: none !important; }
  }

  /* --- MAIN CONTENT --- */
  .post-outer {
      background: var(--retro-white);
      padding: 40px;
      margin-bottom: 40px;
      box-shadow: 0 10px 30px rgba(0,0,0,0.05);
      border-radius: 12px;
      border: 1px solid #eee;
      animation: slideIn3D 0.8s cubic-bezier(0.25, 0.46, 0.45, 0.94) both;
      transform-style: preserve-3d;
      transition: transform 0.4s ease, box-shadow 0.4s ease;
  }

  /* KHUSUS HALAMAN POSTINGAN (Single Item): Mengurangi Jarak */
  .item-view .post-outer {
      padding-top: 20px !important; /* Kurangi padding atas */
  }
  
  .item-view .post-meta {
      margin-bottom: 15px !important; /* Rapatkan jarak meta dengan konten */
      border-bottom: 1px dashed #ddd !important;
  }

  .post-outer:hover {
      transform: translateY(-5px) rotateX(1deg);
      box-shadow: 0 20px 40px rgba(0,0,0,0.1);
  }

  .post-title {
      font-family: var(--font-head);
      font-size: 36px;
      margin: 0 0 10px 0;
      line-height: 1.3;
      font-weight: 700;
  }
  
  .post-title a { color: var(--ink-black); }
  
  .post-meta {
      font-size: 14px;
      color: #777;
      font-style: italic;
      margin-bottom: 25px;
      display: block;
      border-bottom: 1px solid #eee;
      padding-bottom: 15px;
  }

  .post-body {
      font-size: 18px;
      text-align: justify;
      color: #333;
  }

  /* --- STYLE TAGS / LABELS (CHIPS) --- */
  .post-tags-modern {
      display: flex;
      flex-wrap: wrap;
      gap: 8px;
      margin-bottom: 20px; /* Jarak ke konten di bawahnya */
      margin-top: 5px;
  }
  
  .tag-chip {
      display: inline-block;
      font-size: 10px;
      font-family: 'Merriweather', serif;
      font-weight: bold;
      text-transform: uppercase;
      color: #555;
      background: #f0f0f0;
      padding: 4px 12px;
      border-radius: 20px;
      border: 1px solid #e0e0e0;
      letter-spacing: 0.5px;
      transition: all 0.2s ease;
  }
  
  .tag-chip:hover {
      background: var(--ink-black);
      color: #fff;
      transform: translateY(-2px);
      box-shadow: 0 3px 6px rgba(0,0,0,0.1);
  }

  .post-snippet-content {
      display: -webkit-box;
      -webkit-line-clamp: 4;
      -webkit-box-orient: vertical;
      overflow: hidden;
      text-overflow: ellipsis;
      margin-bottom: 15px;
      color: #555;
  }

  .jump-link {
      margin-top: 30px;
      text-align: center;
  }

  .jump-link a {
      display: inline-block;
      background: var(--ink-black);
      color: #fff;
      padding: 10px 30px;
      border-radius: 30px;
      font-weight: bold;
      font-size: 14px;
      letter-spacing: 1px;
      text-transform: uppercase;
      box-shadow: 0 5px 15px rgba(44, 44, 44, 0.3);
      transition: transform 0.3s ease;
  }

  .jump-link a:hover {
      transform: scale(1.1) rotate(-2deg);
      background: #000;
      box-shadow: 0 8px 20px rgba(0,0,0,0.4);
  }

  /* --- STYLE NUMBERED PAGINATION (SIMPLIFIED) --- */
  #blog-pager {
      clear: both;
      text-align: center;
      margin: 40px 0;
      display: flex;
      justify-content: center;
      align-items: center;
      gap: 15px; /* Jarak antar tombol */
  }
  
  /* Tombol Prev/Next (Icon) */
  .pager-btn {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      width: 45px;
      height: 45px;
      border-radius: 50%;
      background: #ffffff;
      border: 2px solid #2c2c2c;
      color: #2c2c2c;
      text-decoration: none;
      font-size: 20px;
      font-weight: bold;
      transition: all 0.3s cubic-bezier(0.175, 0.885, 0.32, 1.275);
      box-shadow: 0 4px 0 #2c2c2c;
  }
  
  .pager-btn:hover {
      transform: translateY(-3px);
      background: #2c2c2c;
      color: #fff;
      box-shadow: 0 6px 0 rgba(0,0,0,0.2);
  }
  
  .pager-btn:active {
      transform: translateY(0);
      box-shadow: 0 2px 0 rgba(0,0,0,0.2);
  }

  /* Angka di Tengah */
  .pager-num {
      font-family: var(--font-head);
      font-size: 24px;
      font-weight: 700;
      color: #2c2c2c;
      min-width: 30px;
      text-align: center;
  }

  /* --- FOOTER (Transparent Background) --- */
  .footer-wrapper {
      text-align: center;
      padding: 40px 20px;
      border-top: 3px double var(--ink-black);
      background: transparent; /* KEMBALI TRANSPARAN */
      margin-top: 40px;
      box-shadow: none; /* HAPUS SHADOW KOTAK */
  }
  
  .footer-icons {
    margin-bottom: 20px;
    display: flex;
    justify-content: center;
    flex-wrap: wrap;
    gap: 20px;
  }
  
  /* Icon Footer Clean */
  .footer-icons a img {
    width: 30px;
    height: 30px;
    border: none; /* Jamin tidak ada border */
    transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
  }
  
  .footer-icons a:hover img {
    transform: scale(1.5) rotate(360deg);
  }

  .Attribution { font-size: 14px; color: #777; }
  .Attribution svg { display: none; }

  /* --- FLOATING WIDGETS --- */
  .translate-floating, .profile-widget-container { z-index: 10000; perspective: 1000px; }

  .translate-floating { 
      position: fixed; bottom: 24px; left: 24px; 
      animation: float3D 4s ease-in-out infinite; 
  }
  
  .translate-btn {
      background-color: #ffffff; border: none; color: #3b82f6;
      padding: 10px; border-radius: 50%; 
      box-shadow: 0 10px 25px rgba(59, 130, 246, 0.3);
      cursor: pointer; width: 50px; height: 50px; 
      display: flex; align-items: center; justify-content: center;
      transition: transform 0.3s;
  }
  .translate-btn:hover { transform: scale(1.2) rotate(15deg); }

  #google_translate_element {
      position: absolute; bottom: 70px; left: 0; 
      background: rgba(255,255,255,0.95); backdrop-filter: blur(10px);
      padding: 15px; border-radius: 12px; 
      box-shadow: 0 20px 40px rgba(0,0,0,0.2); 
      display: none; min-width: 180px;
      transform-origin: bottom left;
  }
  #google_translate_element.active { display: block; animation: pop3D 0.5s cubic-bezier(0.175, 0.885, 0.32, 1.275); }
  
  /* CONTAINER PROFILE WIDGET */
  .profile-widget-container {
      position: fixed; 
      bottom: 24px; /* Default Desktop Position */
      right: 24px;
      display: flex; flex-direction: column; align-items: flex-end;
      /* animation: float3D 5s ease-in-out infinite reverse;  <- Optional animation */
  }
  
  .btn-toggle {
      width: 65px; height: 65px; 
      border: none;
      cursor: pointer; padding: 0;
      overflow: hidden; border-radius: 50%; 
      box-shadow: 0 10px 25px rgba(0,0,0,0.3);
      transition: transform 0.3s;
      background: white;
  }
  .btn-toggle:hover { transform: scale(1.2) rotate(-15deg); }
  
  .profile-thumb { width: 100%; height: 100%; object-fit: cover; }
  
  #portfolioContent {
      width: 300px; padding: 25px; position: relative; margin-bottom: 20px;
      display: none; 
      background: rgba(255,255,255,0.98); backdrop-filter: blur(5px);
      border-radius: 20px;
      box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.25); 
      border: 1px solid rgba(255,255,255,0.5);
      transform-origin: bottom right;
  }
  
  #portfolioContent.fade-in { 
      display: block; 
      animation: smoothUp 0.4s cubic-bezier(0.25, 0.8, 0.25, 1); 
  }
  
  .btn-close { 
      position: absolute; top: 10px; right: 15px; border: none; background: none; 
      font-size: 24px; cursor: pointer; color: #999; transition: color 0.3s;
  }
  .btn-close:hover { color: #ff4757; }

  .profile-main-img { 
      width: 90px; height: 90px; border-radius: 50%; display: block; margin: 0 auto 15px; 
      border: none;
      box-shadow: 0 5px 15px rgba(0,0,0,0.1);
      transition: transform 0.5s;
  }
  .profile-main-img:hover { transform: rotateY(180deg); }

  .social-container { display: flex; justify-content: center; gap: 15px; margin-top: 20px; }
  .icon-social-link img { width: 32px; height: 32px; border: none; transition: transform 0.3s back-out; }
  .icon-social-link:hover img { transform: translateY(-5px) scale(1.2); }

  #custom-toast {
        visibility: hidden; min-width: 280px; 
        background-color: #222; color: #fff;
        text-align: center; border-radius: 50px; padding: 15px 25px; 
        position: fixed; z-index: 2147483647; 
        left: 50%; top: -100px; transform: translateX(-50%) rotateX(-90deg); 
        box-shadow: 0px 15px 30px rgba(0,0,0,0.4); font-size: 14px; font-weight: bold;
        transition: all 0.6s cubic-bezier(0.68, -0.55, 0.265, 1.55);
        opacity: 0;
        display: flex; align-items: center; justify-content: center; gap: 12px;
        border-bottom: 4px solid #ffb6c1;
        transform-origin: top center;
    }
    
    #custom-toast.show { 
        visibility: visible; top: 40px; opacity: 1; 
        transform: translateX(-50%) rotateX(0deg); 
    }
    .toast-icon { width: 28px; height: 28px; fill: #ffb6c1; animation: spin 2s linear infinite; }
    
    @keyframes spin { 100% { transform: rotate(360deg); } }

  /* =========================================
     2. TAMPILAN MOBILE (SMARTPHONE)
     ========================================= */
  @media screen and (max-width: 600px) {
      /* General Mobile Adjustments */
      .main-wrapper { padding: 0 15px; margin-top: 20px; }
      .Header h1 { font-size: 32px; }
      
      /* FIX SPACING & RESPONSIVE FOR PAGES */
      .post-outer { 
          padding: 15px !important; /* Padding dikurangi biar lega */
      }
      
      /* Hapus padding atas khusus untuk halaman statis agar tidak ada gap besar */
      .item-view .post-outer { padding-top: 0px !important; }
      
      .post-title { font-size: 24px; }
      
      /* SEMBUNYIKAN MENU & NAVBAR BAWAAN */
      .nav-wrapper, .PageList { display: none !important; }

      /* --- FLOATING WIDGETS POSITION FIX --- */
      .translate-floating { bottom: 24px; left: 1rem; }
      
      /* Fix Profile Widget Position & Size */
      .profile-widget-container { bottom: 24px !important; right: 24px !important; }
      .btn-toggle { width: 50px !important; height: 50px !important; }
      
      #portfolioContent { 
          width: 260px; /* Fix lebar */
          max-width: 80vw; 
          padding: 15px; 
          margin-bottom: 10px;
      }
      #portfolioContent h2 { font-size: 1rem; margin: 5px 0; }
      #portfolioContent p, #portfolioContent a { font-size: 0.85rem; }
      .profile-main-img { width: 70px; height: 70px; margin-bottom: 10px; }

      /* --- TOMBOL FLOATING MENU (POJOK KIRI ATAS) --- */
      #mobile-floating-menu-btn {
        position: fixed;
        top: 20px;
        left: 20px; /* Pojok Kiri */
        z-index: 99999;
        width: 45px;
        height: 45px;
        background: #2c2c2c;
        color: #fcf8e3;
        border-radius: 50%;
        border: 2px solid #fcf8e3;
        box-shadow: 0 4px 10px rgba(0,0,0,0.3);
        display: flex;
        align-items: center;
        justify-content: center;
        cursor: pointer;
        transition: transform 0.2s;
        -webkit-tap-highlight-color: transparent;
      }

      #mobile-floating-menu-btn:active { transform: scale(0.9); }
      #mobile-floating-menu-btn svg { width: 24px; height: 24px; fill: currentColor; }

      /* --- POPUP MENU --- */
      #mobile-menu-overlay {
        position: fixed;
        top: 75px;
        left: 20px; /* Pojok Kiri */
        width: 200px;
        background: rgba(255, 255, 255, 0.98);
        backdrop-filter: blur(10px);
        border-radius: 12px;
        padding: 10px;
        box-shadow: 0 10px 30px rgba(0,0,0,0.2);
        border: 1px solid rgba(0,0,0,0.1);
        z-index: 99998;
        
        visibility: hidden;
        opacity: 0;
        transform: translateY(-10px) scale(0.95);
        transition: all 0.2s cubic-bezier(0.25, 0.8, 0.25, 1);
        transform-origin: top left;
      }

      #mobile-menu-overlay.active {
        visibility: visible;
        opacity: 1;
        transform: translateY(0) scale(1);
      }

      /* List Menu Mobile */
      .mobile-menu-list {
        list-style: none;
        padding: 0;
        margin: 0;
        display: flex;
        flex-direction: column;
        gap: 5px;
      }

      .mobile-menu-list li a {
        display: block;
        padding: 12px 15px;
        background: transparent;
        border-radius: 8px;
        color: #333;
        text-decoration: none;
        font-weight: bold;
        font-size: 13px;
        text-align: left;
        border-bottom: 1px solid #f0f0f0;
        font-family: sans-serif;
      }
      .mobile-menu-list li:last-child a { border-bottom: none; }
      .mobile-menu-list li a:active, .mobile-menu-list li a:hover {
        background: #f5f5f5; color: #000;
      }
  }

  .widget-item-control, .item-control, .quickedit, .home-link, .feed-links { display: none !important; }
  ]]></b:skin>
</head>

<body>
  <!-- MOBILE FLOATING MENU HTML (Added Here) -->
  <div id='mobile-floating-menu-btn'>
    <svg viewBox='0 0 24 24'>
      <path d='M3 18h18v-2H3v2zm0-5h18v-2H3v2zm0-7v2h18V6H3z'/>
    </svg>
  </div>

  <div id='mobile-menu-overlay'>
    <ul class='mobile-menu-list' id='cloned-menu-list'>
      <li style='padding:10px; text-align:center; color:#999;'>Loading...</li>
    </ul>
  </div>

  <div class='main-wrapper'>
    
    <!-- HEADER SECTION -->
    <header class='header-wrapper'>
      <b:section id='Header' maxwidgets='1' showaddelement='false'>
        <b:widget id='Header1' locked='true' title='Kurniaditya Gumelar (Header)' type='Header' version='2' visible='true'>
          <b:widget-settings>
            <b:widget-setting name='displayUrl'/>
            <b:widget-setting name='displayHeight'>0</b:widget-setting>
            <b:widget-setting name='sectionWidth'>-1</b:widget-setting>
            <b:widget-setting name='useImage'>false</b:widget-setting>
            <b:widget-setting name='shrinkToFit'>false</b:widget-setting>
            <b:widget-setting name='imagePlacement'>BEHIND</b:widget-setting>
            <b:widget-setting name='displayWidth'>0</b:widget-setting>
          </b:widget-settings>
          <b:includable id='main'>
            <div class='header-inner'>
              <h1>
                <b:if cond='data:blog.url == data:blog.homepageUrl'>
                  <data:title/>
                <b:else/>
                  <a expr:href='data:blog.homepageUrl'><data:title/></a>
                </b:if>
              </h1>
              <b:if cond='data:description'>
                <p class='description'><data:description/></p>
              </b:if>
            </div>
          </b:includable>
          <b:includable id='behindImageStyle'>
    <b:if cond='data:sourceUrl'>
      <b:include cond='data:this.image' data='{                    image: data:this.image,                    selector: &quot;.header-widget&quot;                  }' name='responsiveImageStyle'/>
      <style type='text/css'>
        .header-widget {
          background-position: <data:blog.locale.languageAlignment/>;
          background-repeat: no-repeat;
          background-size: cover;
        }
      </style>
    </b:if>
  </b:includable>
          <b:includable id='description'>
    <p>
      <data:this.description/>
    </p>
  </b:includable>
          <b:includable id='image'>
    <a class='header-image-wrapper' expr:href='data:blog.homepageUrl'>
      <b:include data='{                                                 image: data:image,                                                 altText: data:blog.title.escaped,                                                 originalWidth: data:width,                                                 originalHeight: data:height                                               }' name='responsiveImage'/>
    </a>
  </b:includable>
          <b:includable id='title'>
    <h1>
      <b:tag cond='data:view.url != data:blog.homepageUrl' expr:href='data:blog.homepageUrl' name='a'>
        <data:title/>
      </b:tag>
    </h1>
  </b:includable>
        </b:widget>
      </b:section>
    </header>

    <!-- NAVIGATION SECTION -->
    <nav class='nav-wrapper'>
      <b:section id='PageList' maxwidgets='1' showaddelement='true'>
        <b:widget id='PageList1' locked='false' title='Pages' type='PageList' version='2' visible='true'>
          <b:widget-settings>
            <b:widget-setting name='pageListJson'><![CDATA[{"link1":{"href":"/p/design.html","position":1,"title":"DESIGN"},"link0":{"href":"/","position":0,"title":"BERANDA"},"link5":{"href":"/p/retro-tv.html","position":5,"title":"RETRO TV"},"link4":{"href":"/p/about-me.html","position":4,"title":"TENTANG SAYA"},"link3":{"href":"/p/donasi.html","position":3,"title":"DONASI"},"link2":{"href":"/p/youtube.html","position":2,"title":"VIDEO"}}]]></b:widget-setting>
            <b:widget-setting name='homeTitle'>Beranda</b:widget-setting>
          </b:widget-settings>
          <b:includable id='main'>
            <div class='widget-content'>
              <ul>
                <b:loop values='data:links' var='link'>
                  <li expr:class='data:link.isCurrentPage ? &quot;selected&quot; : &quot;&quot;'>
                    <a expr:href='data:link.href'><data:link.title/></a>
                  </li>
                </b:loop>
              </ul>
            </div>
          </b:includable>
          <b:includable id='content'>
  <div class='widget-content'>
    <b:include name='pageList'/>
  </div>
</b:includable>
          <b:includable id='overflowButton'>
  <b:include name='verticalMoreIcon'/>
</b:includable>
          <b:includable id='overflowablePageList'>
  <div class='overflowable-container'>
    <div class='overflowable-contents'>
      <div class='container'>
        <b:with value='true' var='overflow'>
        <b:with value='&quot;tabs&quot;' var='pageListClass'>
          <b:include name='pageList'/>
        </b:with>
        </b:with>
      </div>
    </div>
    <div class='overflow-button hidden'>
      <b:include name='overflowButton'/>
    </div>
  </div>
</b:includable>
          <b:includable id='pageLink'>
  <li>
    <b:class cond='data:overflow' name='overflowable-item'/>
    <b:class cond='data:link.isCurrentPage' name='selected'/>

    <a expr:href='data:link.href'><data:link.title/></a>
  </li>
</b:includable>
          <b:includable id='pageList'>
  <ul>
    <b:class cond='data:pageListClass' expr:name='data:pageListClass'/>
    <b:loop values='data:links' var='link'>
      <b:include name='pageLink'/>
    </b:loop>
  </ul>
</b:includable>
        </b:widget>
      </b:section>
    </nav>

    <!-- MAIN BLOG POSTS -->
    <main class='content-wrapper'>
      <b:section id='Main' showaddelement='true'>
        <b:widget id='Blog1' locked='true' title='Postingan Blog' type='Blog' version='2' visible='true'>
          <b:widget-settings>
            <b:widget-setting name='showDateHeader'>false</b:widget-setting>
            <b:widget-setting name='style.textcolor'>#ffffff</b:widget-setting>
            <b:widget-setting name='showShareButtons'>true</b:widget-setting>
            <b:widget-setting name='showCommentLink'>true</b:widget-setting>
            <b:widget-setting name='style.urlcolor'>#ffffff</b:widget-setting>
            <b:widget-setting name='showAuthor'>true</b:widget-setting>
            <b:widget-setting name='style.linkcolor'>#ffffff</b:widget-setting>
            <b:widget-setting name='style.unittype'>TextAndImage</b:widget-setting>
            <b:widget-setting name='style.bgcolor'>#ffffff</b:widget-setting>
            <b:widget-setting name='timestampLabel'/>
            <b:widget-setting name='reactionsLabel'/>
            <b:widget-setting name='showAuthorProfile'>false</b:widget-setting>
            <b:widget-setting name='style.layout'>1x1</b:widget-setting>
            <b:widget-setting name='showLabels'>true</b:widget-setting>
            <b:widget-setting name='showLocation'>false</b:widget-setting>
            <b:widget-setting name='postLabelsLabel'/>
            <b:widget-setting name='showTimestamp'>true</b:widget-setting>
            <b:widget-setting name='postsPerAd'>1</b:widget-setting>
            <b:widget-setting name='showBacklinks'>false</b:widget-setting>
            <b:widget-setting name='style.bordercolor'>#ffffff</b:widget-setting>
            <b:widget-setting name='showInlineAds'>false</b:widget-setting>
            <b:widget-setting name='showReactions'>false</b:widget-setting>
          </b:widget-settings>
          <b:includable id='main' var='this'>
            <!-- Loop Postingan -->
            <b:loop values='data:posts' var='post'>
              <article class='post-outer'>
                
                <!-- Judul Post (Disembunyikan di Halaman Statis / Pages) -->
                <b:if cond='!data:view.isPage'>
                  <h2 class='post-title'>
                    <b:if cond='data:post.link or (data:post.url and data:view.url != data:post.url)'>
                      <a expr:href='data:post.link ?: data:post.url'><data:post.title/></a>
                    <b:else/>
                      <data:post.title/>
                    </b:if>
                  </h2>
                </b:if>

                <!-- Info Tanggal & Penulis (Hanya muncul jika BUKAN Halaman Statis) -->
                <b:if cond='!data:view.isPage'>
                  <div class='post-meta'>
                    <span>Ditulis oleh <data:post.author.name/></span> | 
                    <span><data:post.date/></span>
                  </div>
                </b:if>

                <!-- TAGS / LABEL BARU (Clean Chips) -->
                <b:if cond='data:post.labels'>
                  <div class='post-tags-modern'>
                    <b:loop values='data:post.labels' var='label'>
                      <a class='tag-chip' expr:href='data:label.url'><data:label.name/></a>
                    </b:loop>
                  </div>
                </b:if>

                <!-- Isi Postingan (Logika Snippet vs Full) -->
                <div class='post-body'>
                  <b:if cond='data:view.isMultipleItems'>
                    <!-- Tampilan di Beranda: Pake CSS Truncate (Potong) -->
                    <div class='post-snippet-content'>
                        <b:if cond='data:post.snippet'>
                           <data:post.snippet/>
                        <b:else/>
                           <!-- Jika tidak ada snippet, tampilkan body dengan limit CSS -->
                           <data:post.body/> 
                        </b:if>
                    </div>
                  <b:else/>
                    <!-- Tampilan di Halaman Post: Full Konten -->
                    <data:post.body/>
                  </b:if>
                </div>

                <!-- Tombol Read More (Hanya muncul di Homepage) -->
                <b:if cond='data:view.isMultipleItems'>
                   <div class='jump-link'>
                     <a expr:href='data:post.url' title='Baca Selengkapnya'>BACA SELENGKAPNYA &#8594;</a>
                   </div>
                </b:if>
                
              </article>
            </b:loop>

            <!-- Paginasi (Riwayat Postingan) -->
            <!-- MENAMPILKAN PAGINASI DI SEMUA HALAMAN KECUALI POSTINGAN TUNGGAL -->
            <b:if cond='!data:view.isSingleItem'>
              <div class='blog-pager' id='blog-pager'>
                <b:if cond='data:newerPageUrl'>
                  <span id='blog-pager-newer-link'>
                    <a class='blog-pager-newer-link' expr:href='data:newerPageUrl' expr:id='data:widget.instanceId + &quot;_blog-pager-newer-link&quot;' expr:title='data:messages.newerPosts'>
                      <data:messages.newerPosts/>
                    </a>
                  </span>
                </b:if>
                <b:if cond='data:olderPageUrl'>
                  <span id='blog-pager-older-link'>
                    <a class='blog-pager-older-link' expr:href='data:olderPageUrl' expr:id='data:widget.instanceId + &quot;_blog-pager-older-link&quot;' expr:title='data:messages.olderPosts'>
                      <data:messages.olderPosts/>
                    </a>
                  </span>
                </b:if>
              </div>
            </b:if>
          </b:includable>
          <b:includable id='aboutPostAuthor'>
  <div class='author-name'>
    <a class='g-profile' expr:href='data:post.author.profileUrl' rel='author' title='author profile'>
      <span>
        <data:post.author.name/>
      </span>
    </a>
  </div>
  <div>
    <span class='author-desc'>
      <data:post.author.aboutMe/>
    </span>
  </div>
</b:includable>
          <b:includable id='addComments'>
  <a expr:href='data:post.commentsUrl' expr:onclick='data:post.commentsUrlOnclick'>
    <b:message name='messages.postAComment'/>
  </a>
</b:includable>
          <b:includable id='blogThisShare'>
  <b:with value='&quot;window.open(this.href, \&quot;_blank\&quot;, \&quot;height=270,width=475\&quot;); return false;&quot;' var='onclick'>
    <b:include name='platformShare'/>
  </b:with>
</b:includable>
          <b:includable id='bylineByName' var='byline'>
  <b:switch var='data:byline.name'>
  <b:case value='share'/>
    <b:include cond='data:post.shareUrl' name='postShareButtons'/>
  <b:case value='comments'/>
    <b:include cond='data:post.allowComments' name='postCommentsLink'/>
  <b:case value='location'/>
    <b:include cond='data:post.location' name='postLocation'/>
  <b:case value='timestamp'/>
    <b:include cond='not data:view.isPage' name='postTimestamp'/>
  <b:case value='author'/>
    <b:include name='postAuthor'/>
  <b:case value='labels'/>
    <!-- Label bawaan disembunyikan karena sudah dipindah ke atas -->
  <b:case value='icons'/>
    <b:include cond='data:post.emailPostUrl' name='emailPostIcon'/>
  </b:switch>
</b:includable>
          <b:includable id='bylineRegion' var='regionItems'>
  <b:loop values='data:regionItems' var='byline'>
    <b:include data='byline' name='bylineByName'/>
  </b:loop>
</b:includable>
          <b:includable id='commentAuthorAvatar'>
  <div class='avatar-image-container'>
    <img class='author-avatar' expr:src='data:comment.authorAvatarSrc' height='35' width='35'/>
  </div>
</b:includable>
          <b:includable id='commentDeleteIcon' var='comment'>
  <span expr:class='&quot;item-control &quot; + data:comment.adminClass'>
    <b:if cond='data:showCmtPopup'>
      <div class='goog-toggle-button'>
        <div class='goog-inline-block comment-action-icon'/>
      </div>
    <b:else/>
      <a class='comment-delete' expr:href='data:comment.deleteUrl' expr:title='data:messages.deleteComment'>
        <img src='https://resources.blogblog.com/img/icon_delete13.gif'/>
      </a>
    </b:if>
  </span>
</b:includable>
          <b:includable id='commentForm' var='post'>
  <div class='comment-form'>
    <a name='comment-form'/>
    <h4 id='comment-post-message'><data:messages.postAComment/></h4>
    <b:if cond='data:this.messages.blogComment != &quot;&quot;'>
      <p><data:this.messages.blogComment/></p>
    </b:if>
    <b:include data='post' name='commentFormIframeSrc'/>
    <iframe allowtransparency='allowtransparency' class='blogger-iframe-colorize blogger-comment-from-post' expr:height='data:cmtIframeInitialHeight ?: &quot;90px&quot;' frameborder='0' id='comment-editor' name='comment-editor' src='' width='100%'/>
    <data:post.cmtfpIframe/>
    <script type='text/javascript'>
      BLOG_CMT_createIframe(&#39;<data:post.appRpcRelayPath/>&#39;);
    </script>
  </div>
</b:includable>
          <b:includable id='commentFormIframeSrc' var='post'>
  <a expr:href='data:post.commentFormIframeSrc' id='comment-editor-src'/>
</b:includable>
          <b:includable id='commentItem' var='comment'>
  <div class='comment' expr:id='&quot;c&quot; + data:comment.id'>
    <b:include cond='data:blog.enabledCommentProfileImages' name='commentAuthorAvatar'/>

    <div class='comment-block'>
      <div class='comment-author'>
        <b:if cond='data:comment.authorUrl'>
          <b:message name='messages.authorSaidWithLink'>
            <b:param expr:value='data:comment.author' name='authorName'/>
            <b:param expr:value='data:comment.authorUrl' name='authorUrl'/>
          </b:message>
        <b:else/>
          <b:message name='messages.authorSaid'>
            <b:param expr:value='data:comment.author' name='authorName'/>
          </b:message>
        </b:if>
      </div>
      <div expr:class='&quot;comment-body&quot; + (data:comment.isDeleted ? &quot; deleted&quot; : &quot;&quot;)'>
        <data:comment.body/>
      </div>
      <div class='comment-footer'>
        <span class='comment-timestamp'>
          <a expr:href='data:comment.url' title='comment permalink'>
            <data:comment.timestamp/>
          </a>
          <b:include data='comment' name='commentDeleteIcon'/>
        </span>
      </div>
    </div>
  </div>
</b:includable>
          <b:includable id='commentList' var='comments'>
  <div id='comments-block'>
    <b:loop values='data:comments' var='comment'>
      <b:include data='comment' name='commentItem'/>
    </b:loop>
  </div>
</b:includable>
          <b:includable id='commentPicker' var='post'>
  <b:if cond='data:post.showThreadedComments'>
    <b:include data='post' name='threadedComments'/>
  <b:else/>
    <b:include data='post' name='comments'/>
  </b:if>
</b:includable>
          <b:includable id='comments' var='post'>
  <section expr:class='&quot;comments&quot; + (data:post.embedCommentForm ? &quot; embed&quot; : &quot;&quot;)' expr:data-num-comments='data:post.numberOfComments' id='comments'>
    <a name='comments'/>
    <b:if cond='data:post.allowComments'>

      <b:include name='commentsTitle'/>

      <div expr:id='data:widget.instanceId + &quot;_comments-block-wrapper&quot;'>
        <b:include cond='data:post.comments' data='post.comments' name='commentList'/>
      </div>

      <b:if cond='data:post.commentPagingRequired'>
        <div class='paging-control-container'>
          <b:if cond='data:post.hasOlderLinks'>
            <a expr:class='data:post.oldLinkClass' expr:href='data:post.oldestLinkUrl'>
              <data:messages.oldest/>
            </a>
            <a expr:class='data:post.oldLinkClass' expr:href='data:post.olderLinkUrl'>
              <data:messages.older/>
            </a>
          </b:if>

          <span class='comment-range-text'>
            <data:post.commentRangeText/>
          </span>

          <b:if cond='data:post.hasNewerLinks'>
            <a expr:class='data:post.newLinkClass' expr:href='data:post.newerLinkUrl'>
              <data:messages.newer/>
            </a>
            <a expr:class='data:post.newLinkClass' expr:href='data:post.newestLinkUrl'>
              <data:messages.newest/>
            </a>
          </b:if>
        </div>
      </b:if>

      <div class='footer'>
        <b:if cond='data:post.embedCommentForm'>
          <b:if cond='data:post.allowNewComments'>
            <b:include data='post' name='commentForm'/>
          <b:else/>
            <data:post.noNewCommentsText/>
          </b:if>
        <b:else/>
          <b:if cond='data:post.allowComments'>
            <b:include data='post' name='addComments'/>
          </b:if>
        </b:if>
      </div>
    </b:if>
    <b:if cond='data:showCmtPopup'>
      <div id='comment-popup'>
        <iframe allowtransparency='allowtransparency' frameborder='0' id='comment-actions' name='comment-actions' scrolling='no'>
        </iframe>
      </div>
    </b:if>
  </section>
</b:includable>
          <b:includable id='commentsLink'>
  <a class='comment-link' expr:href='data:post.commentsUrl' expr:onclick='data:post.commentsUrlOnclick'>
    <b:if cond='data:post.numberOfComments &gt; 0'>
      <b:message name='messages.numberOfComments'>
        <b:param expr:value='data:post.numberOfComments' name='numComments'/>
      </b:message>
    <b:else/>
      <data:messages.postAComment/>
    </b:if>
  </a>
</b:includable>
          <b:includable id='commentsLinkIframe'>
  <!-- G+ comments, no longer available. The includable is retained for backwards-compatibility. -->
</b:includable>
          <b:includable id='commentsTitle'>
  <h3 class='title'><data:messages.comments/></h3>
</b:includable>
          <b:includable id='defaultAdUnit'>
  <ins class='adsbygoogle' data-ad-format='auto' expr:data-ad-client='data:adClientId ?: data:blog.adsenseClientId' expr:data-ad-host='data:blog.adsenseHostId' expr:style='data:style ?: &quot;display: block;&quot;'>
    <b:attr cond='not data:blog.analytics4' expr:value='data:blog.analyticsAccountNumber' name='data-analytics-uacct'/>
  </ins>
  <script>
   (adsbygoogle = window.adsbygoogle || []).push({});
  </script>
</b:includable>
          <b:includable id='emailPostIcon'>
  <span class='byline post-icons'>
    <!-- email post links -->
    <span class='item-action'>
      <a expr:href='data:post.emailPostUrl' expr:title='data:messages.emailPost'>
        <b:include data='{ iconClass: &quot;touch-icon sharing-icon&quot; }' name='emailIcon'/>
      </a>
    </span>
  </span>
</b:includable>
          <b:includable id='facebookShare'>
  <b:with value='&quot;window.open(this.href, \&quot;_blank\&quot;, \&quot;height=430,width=640\&quot;); return false;&quot;' var='onclick'>
    <b:include name='platformShare'/>
  </b:with>
</b:includable>
          <b:includable id='feedLinks'>
  <b:if cond='!data:view.isPost'> <!-- Blog feed links -->
    <b:if cond='data:feedLinks'>
      <div class='blog-feeds'>
        <b:include data='feedLinks' name='feedLinksBody'/>
      </div>
    </b:if>
  <b:else/> <!--Post feed links -->
    <div class='post-feeds'>
      <b:loop values='data:posts' var='post'>
        <b:if cond='data:post.allowComments and data:post.feedLinks'>
          <b:include data='post.feedLinks' name='feedLinksBody'/>
        </b:if>
      </b:loop>
    </div>
  </b:if>
</b:includable>
          <b:includable id='feedLinksBody' var='links'>
  <div class='feed-links'>
  <data:messages.subscribeTo/>
  <b:loop values='data:links' var='f'>
     <a class='feed-link' expr:href='data:f.url' expr:type='data:f.mimeType' target='_blank'><data:f.name/> (<data:f.feedType/>)</a>
  </b:loop>
  </div>
</b:includable>
          <b:includable id='footerBylines'>
  <b:if cond='data:widgets.Blog.first.footerBylines'>
    <b:loop index='i' values='data:widgets.Blog.first.footerBylines' var='region'>
      <b:if cond='not data:region.items.empty'>
        <div expr:class='&quot;post-footer-line post-footer-line-&quot; + (data:i + 1)'>
          <b:with value='&quot;footer-&quot; + (data:i + 1)' var='regionName'>
            <b:include data='region.items' name='bylineRegion'/>
          </b:with>
        </div>
      </b:if>
    </b:loop>
  </b:if>
</b:includable>
          <b:includable id='googlePlusShare'>
</b:includable>
          <b:includable id='headerByline'>
  <b:if cond='data:widgets.Blog.first.headerByline'>
    <div class='post-header'>
      <div class='post-header-line-1'>
        <b:with value='&quot;header-1&quot;' var='regionName'>
          <b:include data='data:widgets.Blog.first.headerByline.items' name='bylineRegion'/>
        </b:with>
      </div>
    </div>
  </b:if>
</b:includable>
          <b:includable id='homePageLink'>
  <a class='home-link' expr:href='data:blog.homepageUrl'>
    <data:messages.home/>
  </a>
</b:includable>
          <b:includable id='iframeComments' var='post'>
  <!-- G+ comments, no longer available. The includable is retained for backwards-compatibility. -->
</b:includable>
          <b:includable id='inlineAd' var='post'>
  <b:if cond='!data:view.isPreview'>
    <b:if cond='data:this.adCode or data:this.adClientId or data:blog.adsenseClientId'>
      <!-- Ad -->
      <div class='inline-ad'>
        <b:if cond='data:this.adCode != &quot;&quot;'>
          <data:this.adCode/>
        <b:else/>
          <b:include cond='data:this.adClientId or data:blog.adsenseClientId' name='defaultAdUnit'/>
        </b:if>
      </div>
    </b:if>
  <b:else/>
    <div class='inline-ad'>
      <div class='inline-ad-placeholder'>
        <span><b:message name='messages.adsGoHere'/></span>
      </div>
    </div>
  </b:if>
</b:includable>
          <b:includable id='linkShare'>
  <b:with value='&quot;window.prompt(\&quot;Copy to clipboard: Ctrl+C, Enter\&quot;, \&quot;&quot; + data:originalUrl + &quot;\&quot;); return false;&quot;' var='onclick'>
    <b:include name='platformShare'/>
  </b:with>
</b:includable>
          <b:includable id='nextPageLink'>
  <a class='blog-pager-older-link' expr:href='data:olderPageUrl' expr:id='data:widget.instanceId + &quot;_blog-pager-older-link&quot;' expr:title='data:messages.olderPosts'>
    <data:messages.olderPosts/>
  </a>
</b:includable>
          <b:includable id='otherSharingButton'>
  <span class='sharing-platform-button sharing-element-other' expr:aria-label='data:messages.shareToOtherApps.escaped' expr:data-url='data:originalUrl' expr:title='data:messages.shareToOtherApps.escaped' role='menuitem' tabindex='-1'>
    <b:with value='{key: &quot;sharingOther&quot;}' var='platform'>
      <b:include name='sharingPlatformIcon'/>
    </b:with>
    <span class='platform-sharing-text'><data:messages.shareOtherApps.escaped/></span>
  </span>
</b:includable>
          <b:includable id='platformShare'>
  <a expr:class='&quot;goog-inline-block sharing-&quot; + data:platform.key' expr:data-url='data:originalUrl' expr:href='data:shareUrl + &quot;&amp;target=&quot; + data:platform.target' expr:onclick='data:onclick ? data:onclick : &quot;&quot;' expr:title='data:platform.shareMessage' target='_blank'>
    <span class='share-button-link-text'>
      <data:platform.shareMessage/>
    </span>
  </a>
</b:includable>
          <b:includable id='post' var='post'>
  <div class='post'>
    <b:include data='post' name='postMeta'/>
    <b:include data='post' name='postTitle'/>
    <b:include name='headerByline'/>
    <b:if cond='data:view.isSingleItem'>
      <b:include data='post' name='postBody'/>
    <b:else/>
      <b:include data='post' name='postBodySnippet'/>
      <b:include data='post' name='postJumpLink'/>
    </b:if>
    <b:include data='post' name='postFooter'/>
  </div>
</b:includable>
          <b:includable id='postAuthor'>
  <span class='byline post-author vcard'>
    <span class='post-author-label'>
      <data:byline.label/>
    </span>
    <span class='fn'>
      <b:if cond='data:post.author.profileUrl'>
        <meta expr:content='data:post.author.profileUrl'/>
        <a class='g-profile' expr:href='data:post.author.profileUrl' rel='author' title='author profile'>
          <span><data:post.author.name/></span>
        </a>
      <b:else/>
        <span><data:post.author.name/></span>
      </b:if>
    </span>
  </span>
</b:includable>
          <b:includable id='postBody' var='post'>
  <!-- If metaDescription is empty, use the post body as the schema.org description too, for G+/FB snippeting. -->
  <div class='post-body entry-content float-container' expr:id='&quot;post-body-&quot; + data:post.id'>
    <data:post.body/>
  </div>
</b:includable>
          <b:includable id='postBodySnippet' var='post'>
  <b:include data='post' name='postBody'/>
</b:includable>
          <b:includable id='postCommentsAndAd' var='post'>
  <article class='post-outer-container'>
    <!-- Post title and body -->
    <div class='post-outer'>
      <b:include data='post' name='post'/>
    </div>

    <!-- Comments -->
    <b:include cond='data:view.isSingleItem' data='post' name='commentPicker'/>

    <!-- Show ad inside post container, after comments, if single item. -->
    <b:include cond='data:view.isSingleItem and data:post.includeAd' data='post' name='inlineAd'/>
  </article>

  <!-- Show ad outside post container (between posts) for feed pages. -->
  <b:include cond='data:view.isMultipleItems and data:post.includeAd' data='post' name='inlineAd'/>
</b:includable>
          <b:includable id='postCommentsLink'>
  <b:if cond='data:view.isMultipleItems'>
    <span class='byline post-comment-link container'>
      <b:include cond='data:post.commentSource != 1' name='commentsLink'/>
    </span>
  </b:if>
</b:includable>
          <b:includable id='postFooter' var='post'>
  <div class='post-footer'>
    <b:include name='footerBylines'/>
    <b:include data='post' name='postFooterAuthorProfile'/>
  </div>
</b:includable>
          <b:includable id='postFooterAuthorProfile' var='post'>
  <b:if cond='data:post.author.aboutMe and data:view.isPost'>
    <div class='author-profile'>
      <b:if cond='data:post.author.authorPhoto.url'>
        <img class='author-image' expr:src='data:post.author.authorPhoto.url' width='50px'/>
        <div class='author-about'>
          <b:include data='post' name='aboutPostAuthor'/>
        </div>
      <b:else/>
        <b:include data='post' name='aboutPostAuthor'/>
      </b:if>
    </div>
  </b:if>
</b:includable>
          <b:includable id='postHeader' var='post'>
  <b:include name='headerByline'/>
</b:includable>
          <b:includable id='postJumpLink' var='post'>
  <div class='jump-link flat-button'>
    <a expr:href='data:post.url fragment &quot;more&quot;' expr:title='data:post.title'>
      <b:eval expr='data:blog.jumpLinkMessage'/>
    </a>
  </div>
</b:includable>
          <b:includable id='postLabels'>
  <span class='byline post-labels'>
    <span class='byline-label'><data:byline.label/></span>
    <b:loop index='i' values='data:post.labels' var='label'>
      <a expr:href='data:label.url' rel='tag'>
        <data:label.name/>
      </a>
    </b:loop>
  </span>
</b:includable>
          <b:includable id='postLocation'>
  <span class='byline post-location'>
    <data:byline.label/>
    <a expr:href='data:post.location.mapsUrl' target='_blank'><data:post.location.name/></a>
  </span>
</b:includable>
          <b:includable id='postMeta' var='post'>
  <b:include data='post' name='postMetadataJSON'/>
</b:includable>
          <b:includable id='postMetadataJSONImage'>
  &quot;image&quot;: {
    &quot;@type&quot;: &quot;ImageObject&quot;,
    <b:if cond='data:post.featuredImage.isResizable'>
    &quot;url&quot;: &quot;<b:eval expr='resizeImage(data:post.featuredImage, 1200, &quot;1200:630&quot;)'/>&quot;,
    &quot;height&quot;: 630,
    &quot;width&quot;: 1200
    <b:else/>
    &quot;url&quot;: &quot;https://blogger.googleusercontent.com/img/b/U2hvZWJveA/AVvXsEgfMvYAhAbdHksiBA24JKmb2Tav6K0GviwztID3Cq4VpV96HaJfy0viIu8z1SSw_G9n5FQHZWSRao61M3e58ImahqBtr7LiOUS6m_w59IvDYwjmMcbq3fKW4JSbacqkbxTo8B90dWp0Cese92xfLMPe_tg11g/w1200/&quot;,
    &quot;height&quot;: 348,
    &quot;width&quot;: 1200
    </b:if>
  },
</b:includable>
          <b:includable id='postMetadataJSONPublisher'>
 &quot;publisher&quot;: {
    &quot;@type&quot;: &quot;Organization&quot;,
    &quot;name&quot;: &quot;Blogger&quot;,
    &quot;logo&quot;: {
      &quot;@type&quot;: &quot;ImageObject&quot;,
      &quot;url&quot;: &quot;https://blogger.googleusercontent.com/img/b/U2hvZWJveA/AVvXsEgfMvYAhAbdHksiBA24JKmb2Tav6K0GviwztID3Cq4VpV96HaJfy0viIu8z1SSw_G9n5FQHZWSRao61M3e58ImahqBtr7LiOUS6m_w59IvDYwjmMcbq3fKW4JSbacqkbxTo8B90dWp0Cese92xfLMPe_tg11g/h60/&quot;,
      &quot;width&quot;: 206,
      &quot;height&quot;: 60
    }
  },
</b:includable>
          <b:includable id='postPagination'>
  <div class='blog-pager container' id='blog-pager'>
    <b:include cond='data:newerPageUrl' name='previousPageLink'/>
    <b:include cond='data:olderPageUrl' name='nextPageLink'/>
    <b:include cond='data:view.url != data:blog.homepageUrl' name='homePageLink'/>
  </div>
</b:includable>
          <b:includable id='postReactions'>
  <!-- Reaction feature no longer available. The includable is retained for backwards-compatibility. -->
</b:includable>
          <b:includable id='postShareButtons'>
  <div class='byline post-share-buttons goog-inline-block'>
    <b:with value='data:sharingId ?: ((data:widget.instanceId ?: &quot;sharing&quot;) + &quot;-&quot; + (data:regionName ?: &quot;byline&quot;) + &quot;-&quot; + data:post.id)' var='sharingId'>
      <!-- Note: 'sharingButtons' includable is from the default Sharing widget markup. -->
      <b:include data='{                                                sharingId: data:sharingId,                                                originalUrl: data:post.url,                                                platforms: data:this.sharing.platforms,                                                shareUrl: data:post.shareUrl,                                                shareTitle: data:post.title,                                              }' name='sharingButtons'/>
    </b:with>
  </div>
</b:includable>
          <b:includable id='postTimestamp'>
  <span class='byline post-timestamp'>
    <data:byline.label/>
    <b:if cond='data:post.url'>
      <meta expr:content='data:post.url.canonical'/>
      <a class='timestamp-link' expr:href='data:post.url' rel='bookmark' title='permanent link'>
        <time class='published' expr:datetime='data:post.date.iso8601' expr:title='data:post.date.iso8601'>
          <data:post.date/>
        </time>
      </a>
    </b:if>
  </span>
</b:includable>
          <b:includable id='postTitle' var='post'>
  <a expr:name='data:post.id'/>
  <b:if cond='data:post.title != &quot;&quot;'>
    <h3 class='post-title entry-title'>
      <b:if cond='data:post.link or (data:post.url and data:view.url != data:post.url)'>
        <a expr:href='data:post.link ?: data:post.url'><data:post.title/></a>
      <b:else/>
        <data:post.title/>
      </b:if>
    </h3>
  </b:if>
</b:includable>
          <b:includable id='previousPageLink'>
  <a class='blog-pager-newer-link' expr:href='data:newerPageUrl' expr:id='data:widget.instanceId + &quot;_blog-pager-newer-link&quot;' expr:title='data:messages.newerPosts'>
    <data:messages.newerPosts/>
  </a>
</b:includable>
          <b:includable id='sharingButton'>
  <span expr:aria-label='data:platform.shareMessage' expr:class='&quot;sharing-platform-button sharing-element-&quot; + data:platform.key' expr:data-href='data:shareUrl + &quot;&amp;target=&quot; + data:platform.target' expr:data-url='data:originalUrl' expr:title='data:platform.shareMessage' role='menuitem' tabindex='-1'>
    <b:include name='sharingPlatformIcon'/>
    <span class='platform-sharing-text'><data:platform.name/></span>
  </span>
</b:includable>
          <b:includable id='sharingButtonContent'>
  <div class='flat-icon-button ripple'>
    <b:include name='shareIcon'/>
  </div>
</b:includable>
          <b:includable id='sharingButtons'>
  <div class='sharing' expr:aria-owns='&quot;sharing-popup-&quot; + data:sharingId' expr:data-title='data:shareTitle'>
    <button class='sharing-button touch-icon-button' expr:aria-controls='&quot;sharing-popup-&quot; + data:sharingId' expr:aria-label='data:messages.share.escaped' expr:id='&quot;sharing-button-&quot; + data:sharingId' role='button'>
      <b:include name='sharingButtonContent'/>
    </button>
    <b:include name='sharingButtonsMenu'/>
  </div>
</b:includable>
          <b:includable id='sharingButtonsMenu'>
  <div class='share-buttons-container'>
    <ul aria-hidden='true' class='share-buttons hidden' expr:aria-label='data:messages.share.escaped' expr:id='&quot;sharing-popup-&quot; + data:sharingId' role='menu'>
      <b:loop values='(data:platforms ?: data:blog.sharing.platforms) filter (p =&gt; p.key not in {&quot;blogThis&quot;})' var='platform'>
        <li>
          <b:include name='sharingButton'/>
        </li>
      </b:loop>
      <li aria-hidden='true' class='hidden'>
        <b:include name='otherSharingButton'/>
      </li>
    </ul>
  </div>
</b:includable>
          <b:includable id='sharingPlatformIcon'>
  <b:include data='{ iconClass: (&quot;touch-icon sharing-&quot; + data:platform.key) }' expr:name='data:platform.key + &quot;Icon&quot;'/>
</b:includable>
          <b:includable id='threadedCommentForm' var='post'>
  <div class='comment-form'>
    <a name='comment-form'/>
    <h4 id='comment-post-message'><data:messages.postAComment/></h4>
    <b:if cond='data:this.messages.blogComment != &quot;&quot;'>
      <p><data:this.messages.blogComment/></p>
    </b:if>
    <b:include data='post' name='commentFormIframeSrc'/>
    <iframe allowtransparency='allowtransparency' class='blogger-iframe-colorize blogger-comment-from-post' expr:height='data:cmtIframeInitialHeight ?: &quot;90px&quot;' frameborder='0' id='comment-editor' name='comment-editor' src='' width='100%'/>
    <data:post.cmtfpIframe/>
    <script type='text/javascript'>
      BLOG_CMT_createIframe(&#39;<data:post.appRpcRelayPath/>&#39;);
    </script>
  </div>
</b:includable>
          <b:includable id='threadedCommentJs' var='post'>
  <script async='async' expr:src='data:post.commentSrc' type='text/javascript'/>
  <b:template-script inline='true' name='threaded_comments'/>
  <script type='text/javascript'>
    blogger.widgets.blog.initThreadedComments(
        <data:post.commentJso/>,
        <data:post.commentMsgs/>,
        <data:post.commentConfig/>);
  </script>
</b:includable>
          <b:includable id='threadedComments' var='post'>
  <section class='comments threaded' expr:data-embed='data:post.embedCommentForm' expr:data-num-comments='data:post.numberOfComments' id='comments'>
    <a name='comments'/>

    <b:include name='commentsTitle'/>

    <div class='comments-content'>
      <b:if cond='data:post.embedCommentForm'>
        <b:include data='post' name='threadedCommentJs'/>
      </b:if>
      <div id='comment-holder'>
         <data:post.commentHtml/>
      </div>
    </div>

    <p class='comment-footer'>
      <b:if cond='data:post.allowNewComments'>
        <b:include data='post' name='threadedCommentForm'/>
      <b:else/>
        <data:post.noNewCommentsText/>
      </b:if>
      <b:if cond='data:post.showManageComments'>
        <b:include data='post' name='manageComments'/>
      </b:if>
    </p>

    <b:if cond='data:showCmtPopup'>
      <div id='comment-popup'>
        <iframe allowtransparency='allowtransparency' frameborder='0' id='comment-actions' name='comment-actions' scrolling='no'>
        </iframe>
      </div>
    </b:if>
  </section>
</b:includable>
          <b:includable id='tooltipCss'>
  <!-- LINT.IfChange -->
  <style>
    .post-body a.b-tooltip-container {
      position: relative;
      display: inline-block;
    }

    .post-body a.b-tooltip-container .b-tooltip {
      display: block !important;
      position: absolute;
      top: 100%;
      left: 50%;
      transform: translate(-20%, 1px);
      visibility: hidden;
      opacity: 0;
      z-index: 1;
      transition: opacity 0.2s ease-in-out;
    }

    .post-body a.b-tooltip-container .b-tooltip iframe {
      width: 200px;
      height: 198px;
      max-width: none;
      border: none;
      border-radius: 20px;
      box-shadow: 1px 1px 3px 1px rgba(0, 0, 0, 0.2);
    }

    @media (hover: hover) {
      .post-body a.b-tooltip-container:hover .b-tooltip {
        visibility: visible;
        opacity: 1;
      }
    }
  </style>
  <!-- LINT.ThenChange(//depot/google3/java/com/google/blogger/b2/layouts/widgets/v2-style.css) -->
</b:includable>
        </b:widget>
      </b:section>
    </main>

  </div> <!-- End Main Wrapper -->

  <!-- PROTEKSI WEBSITE TOAST (ANIMASI ANIME) -->
  <div id='custom-toast'>
    <svg class='toast-icon' viewBox='0 0 512 512'><path d='M256 48C141.1 48 48 141.1 48 256s93.1 208 208 208 208-93.1 208-208S370.9 48 256 48zM163.9 250.4c-10.3-7.2-14.2-21.3-8.8-32.7 13.6-28.8 42.1-48.7 75.6-50.2 29.4-1.3 56.6 13.2 72.5 36.8 6 8.9 17.7 11.7 27.1 6.5 10.3-5.6 14.3-18.3 9.2-28.8-22-45.6-68.6-76.1-121.7-73.7-48.8 2.2-90.8 31.4-110.7 73.4-4.7 9.9-2.5 21.8 5.7 29.6 7.4 7.2 18.9 9 28.1 2.6-3.6 2.4-7.3 5.1-10.8 8-6.5 5.2-11.5 11.7-14.7 19.1-3.2 7.5-4.6 15.8-3.9 24.1.6 7.9 2.9 15.7 6.9 22.8 4 7.1 9.6 13.3 16.6 18.1 13.9 9.6 31.5 13.3 48.2 10 16.7-3.3 31.8-13 41.4-26.8 9.6-13.8 13.3-31.5 10-48.2-3.3-16.7-13-31.8-26.8-41.4-6.9-4.8-14.8-8.3-23.1-10.2 10.3-2.8 20.6-5.1 30.8-7zM348.1 250.4c-10.3-7.2-14.2-21.3-8.8-32.7 13.6-28.8 42.1-48.7 75.6-50.2 29.4-1.3 56.6 13.2 72.5 36.8 6 8.9 17.7 11.7 27.1 6.5 10.3-5.6 14.3-18.3 9.2-28.8-22-45.6-68.6-76.1-121.7-73.7-48.8 2.2-90.8 31.4-110.7 73.4-4.7 9.9-2.5 21.8 5.7 29.6 7.4 7.2 18.9 9 28.1 2.6 10.3 2.8 20.6 5.1 30.8 7-8.3 1.9-16.2 5.4-23.1 10.2-13.8 9.6-23.5 24.7-26.8 41.4-3.3 16.7.4 34.4 10 48.2 9.6 13.8 24.7 23.5 41.4 26.8 16.7 3.3 34.4-.4 48.2-10 7-4.8 12.5-11 16.6-18.1 4-7.1 6.3-14.9 6.9-22.8.7-8.3-.7-16.6-3.9-24.1-3.2-7.5-8.2-13.9-14.7-19.1-3.5-2.9-7.2-5.6-10.8-8z'/></svg>
    <span id='toast-text'/>
  </div>

  <script type='text/javascript'>
  //<![CDATA[
    // SCRIPT PERLINDUNGAN WEBSITE (DITINGKATKAN)
    // 1. Toast Logic
    const toast = document.getElementById("custom-toast");
    const toastText = document.getElementById("toast-text");
    let timer;
    const animeQuotes = [
        "Yamete Kudasai! Jangan dicopy, Senpai~",
        "Omae wa mou... tidak bisa klik kanan!",
        "Dilarang mencuri! Itu tindakan villain!",
        "Ara ara~ Mau ngapain tuh?",
        "Nani?! Teknik copy-paste tidak mempan!",
        "Ho? Kamu mencoba mendekat untuk mencuri?"
    ];
    
    function showToast() {
        const randomIndex = Math.floor(Math.random() * animeQuotes.length);
        toastText.textContent = animeQuotes[randomIndex];
        toast.className = "show";
        clearTimeout(timer);
        timer = setTimeout(function(){ toast.className = toast.className.replace("show", ""); }, 3000);
    }

    // 2. Disable Right Click (Context Menu)
    document.addEventListener('contextmenu', function(e) {
        e.preventDefault();
        e.stopPropagation();
        showToast();
        return false;
    }, true);

    // 3. Disable Selection
    document.addEventListener('selectstart', function(e) {
        e.preventDefault();
        return false;
    });

    // 4. Disable Keyboard Shortcuts (F12, Ctrl+U, Ctrl+Shift+I/J/C)
    document.addEventListener('keydown', function(e) {
        // F12
        if (e.keyCode == 123) {
            e.preventDefault();
            e.stopPropagation();
            showToast();
            return false;
        }
        
        // Ctrl+Shift+I, Ctrl+Shift+J, Ctrl+Shift+C (DevTools)
        if (e.ctrlKey && e.shiftKey && (e.keyCode == 73 || e.keyCode == 74 || e.keyCode == 67)) {
            e.preventDefault();
            e.stopPropagation();
            showToast();
            return false;
        }
        
        // Ctrl+U (View Source)
        if (e.ctrlKey && e.keyCode == 85) {
            e.preventDefault();
            e.stopPropagation();
            showToast();
            return false;
        }
        
        // Ctrl+S (Save), Ctrl+P (Print)
        if (e.ctrlKey && (e.keyCode == 83 || e.keyCode == 80)) {
            e.preventDefault();
            e.stopPropagation();
            showToast();
            return false;
        }
    }, true); // Use Capture phase for higher priority

    // 5. Anti-Debugger (Optional - Makes inspect element harder)
    setInterval(function(){
        // Logic: Jika DevTools terbuka, debugger statement akan memberhentikan script
        // Ini tidak menutup DevTools tapi membuatnya sangat mengganggu
        (function(){})['constructor']('debugger')();
    }, 2000);
  //]]>
  </script>

  <!-- SCRIPT FLOATING MENU MOBILE (AUTO-RETRY COPY) -->
  <script type='text/javascript'>
    //<![CDATA[
    (function() {
        const btn = document.getElementById('mobile-floating-menu-btn');
        const menu = document.getElementById('mobile-menu-overlay');
        const targetList = document.getElementById('cloned-menu-list');
        let menuCopied = false;

        // Fungsi untuk meng-copy menu (Coba terus sampai berhasil)
        function copyMenu() {
            const originalList = document.querySelector('.PageList ul');
            
            if (originalList && originalList.children.length > 0) {
                targetList.innerHTML = originalList.innerHTML;
                
                // Bersihkan class/style bawaan agar tampilan mobile bersih
                const links = targetList.querySelectorAll('a');
                links.forEach(a => {
                    a.removeAttribute('class');
                    a.removeAttribute('style');
                });
                
                menuCopied = true;
                return true;
            }
            return false;
        }

        // Coba copy segera
        if(!copyMenu()) {
            // Jika gagal, cek ulang setiap 500ms (maksimal 10 detik)
            const checkInterval = setInterval(() => {
                if (copyMenu()) {
                    clearInterval(checkInterval); // Berhenti jika sudah berhasil
                }
            }, 500);
            
            // Hentikan pengecekan paksa setelah 10 detik untuk hemat memori
            setTimeout(() => clearInterval(checkInterval), 10000);
        }

        // Event Listener Tombol (Lebih responsif daripada onclick html)
        if(btn && menu) {
            btn.addEventListener('click', function(e) {
                e.stopPropagation(); // Mencegah bubbling
                
                // Jika menu belum ter-copy saat diklik (koneksi lambat), coba copy lagi sekarang
                if (!menuCopied) copyMenu();

                // Toggle tampilan
                menu.classList.toggle('active');

                // Ganti Icon
                if (menu.classList.contains('active')) {
                    btn.innerHTML = '<svg viewBox="0 0 24 24"><path d="M19 6.41L17.59 5 12 10.59 6.41 5 5 6.41 10.59 12 5 17.59 6.41 19 12 13.41 17.59 19 19 17.59 13.41 12z" fill="currentColor"/></svg>';
                } else {
                    btn.innerHTML = '<svg viewBox="0 0 24 24"><path d="M3 18h18v-2H3v2zm0-5h18v-2H3v2zm0-7v2h18V6H3z" fill="currentColor"/></svg>';
                }
            });

            // Tutup jika klik di luar
            document.addEventListener('click', function(e) {
                if (!menu.contains(e.target) && !btn.contains(e.target)) {
                    menu.classList.remove('active');
                    // Reset icon ke hamburger
                    btn.innerHTML = '<svg viewBox="0 0 24 24"><path d="M3 18h18v-2H3v2zm0-5h18v-2H3v2zm0-7v2h18V6H3z" fill="currentColor"/></svg>';
                }
            });
        }
    })();
    //]]>
  </script>

  <!-- SCRIPT NUMBERED PAGINATION (SIMPLIFIED & ROBUST) -->
  <script type='text/javascript'>
    //<![CDATA[
    document.addEventListener("DOMContentLoaded", function() {
        const pager = document.getElementById("blog-pager");
        if (!pager) return;

        // Ambil Link Asli Blogger
        const newerLink = document.getElementById("blog-pager-newer-link");
        const olderLink = document.getElementById("blog-pager-older-link");
        
        let newerUrl = newerLink ? newerLink.getElementsByTagName("a")[0].href : null;
        let olderUrl = olderLink ? olderLink.getElementsByTagName("a")[0].href : null;

        // Logika Halaman (Menggunakan Session Storage untuk simulasi angka)
        let currentPage = 1;
        const isHome = window.location.pathname === '/' || window.location.pathname === '/index.html';
        
        if (isHome) {
            sessionStorage.setItem('ngorand_page', 1);
            currentPage = 1;
        } else {
            // Ambil dari session jika ada, atau default ke 2 jika tidak diketahui
            let savedPage = parseInt(sessionStorage.getItem('ngorand_page'));
            if (savedPage) {
                currentPage = savedPage;
            } else {
                // Fallback jika user langsung ke halaman dalam tanpa lewat home
                currentPage = 2; // Asumsi aman
            }
        }

        // Hapus Konten Lama
        pager.innerHTML = "";

        // Buat Tombol PREV [ < ]
        if (newerUrl) {
            const prevBtn = document.createElement("a");
            prevBtn.href = newerUrl;
            prevBtn.className = "pager-btn";
            prevBtn.innerHTML = "«"; // Simbol Panah Kiri
            prevBtn.title = "Halaman Sebelumnya";
            prevBtn.onclick = function() {
                // Decrement page number
                sessionStorage.setItem('ngorand_page', currentPage > 1 ? currentPage - 1 : 1);
            };
            pager.appendChild(prevBtn);
        }

        // Buat Angka Tengah [ 1 ]
        const numDisplay = document.createElement("span");
        numDisplay.className = "pager-num";
        numDisplay.textContent = currentPage;
        pager.appendChild(numDisplay);

        // Buat Tombol NEXT [ > ]
        if (olderUrl) {
            const nextBtn = document.createElement("a");
            nextBtn.href = olderUrl;
            nextBtn.className = "pager-btn";
            nextBtn.innerHTML = "»"; // Simbol Panah Kanan
            nextBtn.title = "Halaman Selanjutnya";
            nextBtn.onclick = function() {
                // Increment page number
                sessionStorage.setItem('ngorand_page', currentPage + 1);
            };
            pager.appendChild(nextBtn);
        }
    });
    //]]>
  </script>

</body>
</html>